# 复制成 .env 再填。.env 不会进 git（见 .gitignore）。
CODEX_DORO_KEY=        # 必填：doro 模型后端 key（OpenAI 兼容）
EVOMAP_TOKEN=          # 可选：EvoMap 开发者 access token（OAuth2+PKCE，scope recipe:read gene:read）
# DORO_BASE_URL=https://proxy.yetian.online/sub2api/v1
# DORO_MODEL=gpt-5.5
